"""
App entrypoint package for Ego4D gaze training with TF+AR loss.

This exists so the standard launcher (`app/main.py`) can run:
  app: vjepa_ego4d_gaze_unified
which will import `app.vjepa_ego4d_gaze_unified.train`.
"""


